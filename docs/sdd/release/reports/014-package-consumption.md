# 014 - Package consumption validation report

## Status

Completed.

## Package

- Package ID: `TypedParameters.Dapper.SqlServer`
- Version: `0.1.0-preview.1`
- Package used: `TypedParameters.Dapper.SqlServer.0.1.0-preview.1.nupkg`

## NuGet sources

- Local package source: `./artifacts/packages`
- NuGet.org source: `https://api.nuget.org/v3/index.json`

## Isolation mechanism

- Temporary validation workspace under `artifacts/package-consumption-*`.
- Temporary `NuGet.Config` with `<clear />` package sources.
- Package source mapping maps `TypedParameters.Dapper.SqlServer` to
  `local-package`.
- NuGet.org remains available for transitive dependencies.
- `NUGET_PACKAGES` points to an isolated cache under the temporary workspace.
- Each consumer references the exact version `0.1.0-preview.1`.
- The restored package file in the isolated cache is SHA-512 compared with the
  local `.nupkg`.
- Temporary validation workspace is removed after successful execution.

## APIs compiled

- `using Dapper.TypedParameters.SqlServer;`
- `SqlParam.VarChar`
- `SqlParam.Decimal`
- `SqlParam.Int`
- `SqlParam.UniqueIdentifier`
- `SqlParam.VarBinary`
- `SqlParam.Date`
- `SqlParam.Time`
- `SqlParam.DateTime2`
- `TypedSqlParameter.AddParameter`
- `TypedSqlParameter.AsOutput`
- `TypedSqlParameter.OutputValue`
- `TypedSqlParameter.GetValue<T>()`
- `SqlParam.TableValued`
- `TableValuedSqlParameter.AddParameter`
- `Microsoft.Data.SqlClient.SqlCommand` materialization
- `Dapper.SqlMapper.ICustomQueryParameter` assignability

## Results

- `net8.0` consumer: passed
- `net10.0` consumer: passed

## Problems found

- First local attempt hit a transient file lock during `dotnet restore` of the
  generated `net8.0` consumer project.
- The script now retries restore up to three times and still fails hard if the
  condition persists.
- No package content, dependency resolution, build, runtime, or public API
  consumption issue remained after the retry guard.

## Conclusion

The generated package
`TypedParameters.Dapper.SqlServer.0.1.0-preview.1.nupkg` can be consumed by
external `net8.0` and `net10.0` console applications without `ProjectReference`,
without direct DLL references, and without copying implementation files. The
target package is resolved from the local package feed generated by the build,
with an isolated NuGet cache and hash verification against the local `.nupkg`.

Package published: No.
