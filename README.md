# Dapper.TypedParameters

Typed SQL Server parameters for Dapper, with explicit database types and sizes.

> The project is currently being bootstrapped. The first implementation will target `Microsoft.Data.SqlClient`.
