# Changelog

## Unreleased

- Added SQL Server temporal parameter factories for `date`, `time`, `datetime`,
  `smalldatetime`, `datetime2`, and `datetimeoffset`.
- Added scale validation for `time`, `datetime2`, and `datetimeoffset`.
- Updated the package target frameworks to `net8.0` and `net10.0`.

